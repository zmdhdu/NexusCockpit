- 所有外部依赖地址通过 .env 注入，docker-compose.yml 中仅引用环境变量，禁止硬编码 IP/端口。
- 可观测性组件（milvus、neo4j、redis、rabbitmq、mysql）均定义 healthcheck，应用服务通过 depends_on + condition: service_healthy 等待就绪。
- Makefile 目标遵循 `<target>: ## <说明>` 注释约定，供 make help 自动生成帮助输出。
- 模型与数据目录在 .gitignore 中以「忽略具体文件但保留空目录结构」的模式管理（!models/asr/ 等）。