本地开发需先 `make install`（CPU 版 PyTorch）或 `make install-gpu`（cu121），再 `docker compose up -d` 拉起中间件，最后 `make init-db` 初始化 Milvus/Neo4j；Windows 上 Redis 映射到宿主机 16379、MySQL 映射到 13306 以避让 Hyper-V 端口保留区与本机 MySQL。