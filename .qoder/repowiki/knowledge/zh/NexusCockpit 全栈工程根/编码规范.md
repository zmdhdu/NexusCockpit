- 所有外部依赖地址（Milvus/Neo4j/Redis/LLM）通过 `.env` 中的 `*_PROVIDER=local|cloud` 开关在本地 Docker 与云端托管之间切换
- 容器间通信一律使用 Docker Compose 网络名加端口（如 `nexus_ai:8000`、`milvus:19530`），禁止硬编码宿主机 IP
- 应用服务通过 `/health` 健康检查端点暴露存活状态，供 Compose healthcheck 探测