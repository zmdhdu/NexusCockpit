- 配置文件采用“每个组件一个目录 + 原生文件名即 key”的扁平布局，便于直接 volume mount 到容器 `/etc/<组件>/` 路径。
- CI 中每个 job 通过 `defaults.run.working-directory` 指定子工程根目录，保持多语言仓库的构建入口一致。
- PowerShell 启动脚本统一命名为 `start-<组件>.ps1`，作为 Windows 开发环境的约定式入口。