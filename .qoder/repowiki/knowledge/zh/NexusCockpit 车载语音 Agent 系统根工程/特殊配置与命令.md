单仓一键启动：`make install && make dev`（或 `make install-gpu` 装 GPU PyTorch）；`docker compose up -d` 拉起 Milvus/Neo4j/Redis/MySQL/Loki/Prometheus/Grafana；`make init-db` 初始化向量库与图数据库；`make check` 一次性跑 lint/mypy/pytest。