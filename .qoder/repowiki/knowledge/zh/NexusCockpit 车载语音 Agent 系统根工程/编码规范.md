- 所有中间件与服务的连接信息统一从根级 `.env` 注入，禁止在代码中硬编码 host/port
- 模型权重与静态资源必须放在 `models/`、`data/`、`assets/` 下并通过 Docker volume 挂载，不得随源码提交二进制大文件
- 新增基础设施组件需在 `docker-compose.yml` 声明 healthcheck 并在依赖服务中使用 `depends_on.condition: service_healthy`
- 后端代码变更需经 `make format && make lint && make test` 三件套验证后再提交