- 所有服务通过根目录 .env 注入环境变量，容器内通过 ${VAR} 引用默认值，开发者覆盖时复制为 .env.local 而非修改 .env
- Docker Compose 使用 profiles 分离应用服务（app）与基础设施服务，健康检查通过 healthcheck + condition: service_healthy 保证依赖就绪
- Makefile 中每个 target 以 ## 注释描述用途，便于 `make help` 自动生成帮助文档
- Windows 开发脚本统一放在 scripts/ 目录下并通过 PowerShell 执行，避免跨平台路径差异