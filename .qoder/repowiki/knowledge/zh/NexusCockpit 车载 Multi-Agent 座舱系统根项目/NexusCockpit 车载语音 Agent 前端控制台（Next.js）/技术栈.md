Next.js 14.2.5 + React 18.3 + TypeScript 5.5；状态管理为自实现的模块级单例（Zustand 风格，见 auth-store.ts）；HTTP 客户端使用 axios（REST）+ 原生 fetch（SSE 流式）；UI 基于 Tailwind CSS 3.4 + clsx + class-variance-authority + lucide-react 图标；3D 可视化使用 three.js + @react-three/fiber + @react-three/drei；图表使用 recharts；通知使用 sonner；Markdown 渲染使用 react-markdown + remark-gfm；动画使用 framer-motion。