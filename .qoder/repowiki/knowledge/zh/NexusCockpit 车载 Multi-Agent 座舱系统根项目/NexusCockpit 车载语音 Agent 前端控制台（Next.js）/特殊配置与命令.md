开发启动 `npm run dev`（端口 3000），构建 `npm run build` 生成 standalone 产物，类型检查 `npm run type-check`。需配置 `.env.local` 中的 `NEXT_PUBLIC_API_URL`（默认 http://localhost:8080）、`NEXT_PUBLIC_DEFAULT_USER` 和 `NEXT_PUBLIC_DEFAULT_PASSWORD`（用于开发环境自动获取 JWT）。Docker 构建使用多阶段 Dockerfile，最终运行 `node server.js` 暴露 3000 端口。