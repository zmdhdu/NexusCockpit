- 每个模型子目录均包含 README.md 作为模型卡片，统一列出下载地址、框架、用途与推理命令。
- 模型权重与配置文件分离存放：`.pt`/`.bin`/`.safetensors` 为权重，`config.json`/`configuration.json`/`*.yaml` 为结构配置，`tokenizer.*` 为分词器资源。
- 支持 ModelScope 与 HuggingFace 双镜像下载，README 中同时给出两种 SDK 命令以便国内网络访问。
- 示例音频/语音样本统一放在子目录的 `example/` 或 `examples/` 文件夹中，便于快速体验推理。