每个子模型的 README 提供三种下载方式：HuggingFace `huggingface-cli download`、ModelScope `modelscope download`、Git LFS `git clone`；CosyVoice 需先 `git submodule update --init --recursive` 再安装 requirements.txt；llama.cpp 子目录已预编译 Windows DLL 与 Linux 可执行文件，可直接运行 `llama-server.exe` 或 `llama-cli.exe` 进行本地推理。