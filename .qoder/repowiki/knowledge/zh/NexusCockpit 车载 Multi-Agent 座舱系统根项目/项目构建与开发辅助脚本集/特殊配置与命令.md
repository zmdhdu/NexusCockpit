Windows 下以管理员权限运行 `scripts/install-autostart.ps1` 注册开机自启任务，`-Remove` 参数可移除；一键启动全部服务用 `scripts/start-all.ps1`（支持 `-NoGateway`、`-NoFrontend` 开关）；单独启动各服务使用对应的 `start-*.ps1`；停止所有服务用 `scripts/stop-all.ps1`；导出 OpenAPI 文档需从 `backend_design` 目录执行 `python -m scripts.export_openapi`。