一键启动基础设施：`docker compose up -d`；开发模式叠加调试配置：`docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d`；统一工程命令通过 `make <target>`（install / dev / test / lint / docker-up / init-db 等）覆盖 Python、Go、Frontend 三类子模块。