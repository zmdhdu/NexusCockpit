除 `.gitkeep` 外本目录文件不纳入 Git 跟踪；各子目录路径需通过对应环境变量（`FOOD_DATA_DIR`、`KNOWLEDGE_DATA_DIR`、`UPLOAD_DIR`、`TEMP_DIR`）配置，默认路径见 `data/README.md`；`checkpoints/` 下的 `nexus_checkpoints.db` 由 LangGraph 自动创建和维护，重启后会话状态不丢失。