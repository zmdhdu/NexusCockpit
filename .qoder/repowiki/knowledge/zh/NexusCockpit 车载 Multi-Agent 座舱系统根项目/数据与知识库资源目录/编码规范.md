- 所有 JSON 数据文件包含元数据字段（如 `updated_at`、`description`、`id` 等）以记录版本和来源信息
- 偏好配置采用分层嵌套结构，将相关设置聚合到语义明确的键下（如 `music`、`food`、`location`、`climate`、`navigation`）
- 数据目录通过环境变量而非硬编码路径进行访问，便于在不同部署环境中切换数据源
- Markdown 知识文档统一使用中文标题和 Q&A 问答体例，便于语音助手直接解析展示