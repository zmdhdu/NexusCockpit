# ============================================================
# NexusCockpit Python Dependencies
# Usage: pip install -r requirements.txt
# Python >= 3.10 required
# ============================================================
# NOTE: PyTorch must be installed separately before this file.
#   CPU:  pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
#   GPU:  pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
# ============================================================

# --- Web Framework ---
fastapi==0.128.0
uvicorn[standard]==0.40.0
python-multipart==0.0.21
websockets==12.0
starlette==0.50.0

# --- LLM / Agent (LangChain + LangGraph 全栈) ---
# 核心编排: LangGraph StateGraph + checkpoint 持久化
# LLM 客户端: langchain-openai ChatOpenAI (替代 openai SDK 直接调用)
# 工具集成: langgraph-prebuilt ToolNode + create_react_agent
# 消息管理: langchain-core trim_messages + BaseMessage
langchain-openai>=1.1.0          # ChatOpenAI + OpenAIEmbeddings (替代 openai SDK)
langchain-core>=1.2.0           # trim_messages, SystemMessage, StructuredTool
langchain-text-splitters>=1.0.0 # RecursiveCharacterTextSplitter
langgraph>=1.0.6                # StateGraph 工作流编排
langgraph-prebuilt>=1.0.0       # ToolNode, create_react_agent
langgraph-checkpoint-sqlite>=2.0.0  # SQLite checkpoint 持久化
# openai 作为 langchain-openai 的传递依赖自动安装，无需显式声明

# --- RAG / Vector Store (LangChain 集成) ---
# 向量存储: langchain-milvus Milvus (替代手写 pymilvus 操作)
# 图谱存储: langchain-neo4j Neo4jGraph (替代手写 neo4j driver)
# 检索器: langchain-community BM25Retriever (替代手写 rank_bm25)
# Rerank: sentence-transformers bge-reranker-v2-m3
langchain-milvus>=0.4.0         # LangChain Milvus 向量存储集成
langchain-neo4j>=0.10.0         # LangChain Neo4j 图谱存储集成
langchain-community>=0.4.0      # BM25Retriever, Neo4jGraph (社区集成包)
pymilvus>=3.0.0                 # Milvus Python SDK (langchain-milvus 依赖)
neo4j>=5.18.0                   # Neo4j Python Driver (langchain-neo4j 依赖)
sentence-transformers>=2.7.0   # 本地 Embedding (bge-m3) + Reranker (bge-reranker)

# --- Middleware ---
redis==5.0.0

# --- Database (MySQL async + ORM) ---
aiomysql>=0.2.0                 # MySQL 异步驱动
sqlalchemy>=2.0.0               # ORM (后续 db_manager 迁移用)

# --- MCP (Model Context Protocol) ---
mcp>=2.0.0                      # MCP SDK (车控服务标准协议)

# --- Config / Validation ---
pydantic==2.12.5
pydantic-core==2.41.5
pydantic-settings>=2.1.0
python-dotenv==1.2.1

# --- Observability ---
langfuse==4.5.0
prometheus-client==0.23.1
structlog>=24.1.0

# --- Auth ---
PyJWT>=2.10.0
passlib[bcrypt]>=1.7.4

# --- Audio / ASR / TTS ---
funasr==1.1.12
modelscope==1.15.0
torchaudio>=2.2.0
librosa==0.11.0
soundfile==0.13.1
pydub==0.25.1

# --- HTTP / Network ---
httpx==0.28.1
requests==2.32.5
aiohttp==3.13.3

# --- Utils ---
orjson==3.11.5
tiktoken==0.12.0                # token 计数 (langchain-core trim_messages 依赖)
PyYAML==6.0.3

# --- Search ---
tavily==1.1.0
duckduckgo-search>=4.0.0

# --- Data Processing ---
numpy==1.26.4
pandas==2.3.3
scikit-learn==1.7.2

# --- NLP ---
sentencepiece==0.2.1
transformers==4.45.2
tokenizers==0.20.3

# --- Dev Tools ---
pytest==9.0.2
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
ruff>=0.3.0
mypy>=1.8.0
