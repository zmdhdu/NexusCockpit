#  NexusCockpit — 车载离线 Multi-Agent 语音系统  
> **项目**: NexusCockpit — 车载离线 Multi-Agent 语音系统  
> **项目地址**: D:\zhangmengdi\WorkSpace\NexusCockpit
> **运行环境**: Windows 10/11 x64 + NVIDIA RTX4070 (CUDA) 本地仿真车机
> **运行环境**: (nexus) PS D:\zhangmengdi\WorkSpace\NexusCockpit> + docker
## 角色
车载Multi-Agent项目全域文档标准化工程师，项目基础信息：车载本地部署Multi-Agent智能语音系统，基于LangChain/LangGraph构建，Supervisor-Dispatch多层级Agent架构、并行Sub-Agent专家体系、MCP协同、分层长短记忆、车载车控Skill插件体系；运行环境Windows+RTX4070仿真车机。
重要前置背景：项目已经完成大规模底层代码整改：修复路由漂移、车控指令异常、多需求并行调度缺陷、代码注释精简、冗余代码清理、低收益笨重模块降级裁撤；全程底层代码修复，拒绝依靠堆砌Prompt掩盖程序缺陷。

## 核心硬性全局规则
1. **全文禁止写入任何版本号**，所有md文档、代码注释、配置说明移除全部版本标记，保持长期可迭代一致性；
2. 所有文档内容必须与【当前整改完成后的源码逻辑100%对齐】，删除过期架构描述、废弃流程、旧BUG说明、历史临时方案；
3. 保持文档检索友好性：分层清晰、模块标题明确，后续调整代码时，可以通过文档关键词快速定位对应代码文件、模块路径；
4. 区分四类文档分别专项更新：各级README、架构设计文档、Harness Engineering范式Agent设计文档、模块专项文档、memory踩坑备忘文档、简历项目介绍文档；
5. 源码注释沿用统一简化规范：简化大段流程型冗余注释，不要修改目前源码逻辑；
6. 不修改任何业务代码逻辑，仅同步更新注释与配套文档文字；
7. 文档避免过度空洞描述，偏向工程落地视角，突出企业级落地价值，兼顾面试技术讲解需求。

## 一、各级README.md 更新规范
1. 根目录README：更新项目整体架构、核心能力清单、项目亮点、部署流程、依赖说明、环境启动步骤；删除过时部署命令；
2. 子模块目录README（agent/skill/memory/dispatch/车控模块等）：每个子目录README写明：模块定位、核心职责、对外依赖、入口文件、关键能力、关联业务场景；方便后续代码改动快速检索定位；
3. 统一修正链路描述：supervisor → dispatch → responder → reflection → reviewer 完整流水线，准确描述并行Sub-Agent调度机制。

## 二、专项架构md文档更新（主Agent文档、子Agent文档、Harness Engineering范式文档）
1. `主agent.md`：更新顶层调度架构、Supervisor职责、任务分发策略、全局状态机、任务生命周期管理（AI生成任务隔离机制，仅手动暂停可终止）；
2. 各级Sub-Agent专家文档（车控Agent、天气、多媒体、查询专家等）：更新每个子Agent能力边界、支持指令集合、路由匹配规则、入参出参结构体、异常处理逻辑；
3. `harness 编程范式.md`：更新本项目落地的Harness工程范式实现方式，明确分层校验机制、输出拦截规则、流水线强制闭环逻辑，删除过时设计思路；
4. 修正所有架构流程图文字说明，同步匹配修复后的多任务并行调度、双向对话持久化存储、车控指令参数校验等新能力。

## 三、memory.md（项目踩坑备忘文档，新建会话防重复踩坑）
完善并持续补充，作为后续与AI新开会话调试项目的参考基线，内容包含：
1. 历史重大底层问题清单 + 最终根因与解决方案：
   - 天气路由错误、空调指令路由漂移至导航
   - 对话存储仅保存用户消息，缺少AI应答持久化
   - 单条复合需求只能执行单一任务，并行调度失效
   - 界面切换强制终止LLM生成，任务生命周期管控缺陷
   - 车窗仅支持全开全关，缺少单窗/多窗组合控制
   - 空调空参数、非法参数下发引发异常
   - 大量冗长注释，统一精简规范
2. 项目改造红线约束：禁止依靠增加Prompt修复BUG，所有异常优先底层代码整改；
3. 模块取舍标准：低收益、负干扰、笨重模块执行降级/裁撤策略；
4. 代码注释统一标准、文档编写规范；
5. 常见调试日志关键字、路由排查方式、并行任务测试用例参考；
> 作用：后续开启全新会话让AI修改代码时，直接参考本文件，避免重复踩历史坑，无需反复复述项目历史问题。

## 四、简历配套项目文档（简历md，重点完善，面向求职面试）
> 目标：内容详实、覆盖全部技术细节，形成完整知识库，面试所有可被提问的技术点全部留存文档，方便随时查阅准备。
1. 项目概述：场景（车载本地智能语音Multi-Agent）、硬件环境、技术栈（LangChain、LangGraph、MCP、自定义Sub-Agent、分层记忆、RAG、车控Skill插件）；
2. 核心架构设计：完整Multi-Agent五层流水线、并行专家调度方案、任务池管理、状态流转机制；
3. 重点难点与解决方案（重中之重，全部写清底层实现，面试官高频提问点）：
   - 复合多需求原子任务拆解、多Sub-Agent并发调度实现方案；
   - 路由漂移问题底层优化手段、意图匹配权重设计；
   - 对话记忆分层设计（短期会话记忆+长期用户习惯记忆，双向消息持久化）；
   - Harness工程范式落地、全链路输出校验机制，防止绕过链路裸输出；
   - AI流式生成任务生命周期隔离方案；
   - 车载车控指令标准化、参数校验、硬件互斥管控；
4. 项目优化工作：冗余代码清理、注释标准化、臃肿模块精简瘦身；
5. 项目创新点、相较于传统单Agent车载语音方案优势（用来吸引面试官）；
6. 可拓展方向、后续演进思路；
文档语言兼顾两种阅读场景：①自己快速查阅复习；②可直接节选内容粘贴进正式简历。

## 五、其余全部配套文档统一同步更新
接口文档、技能Skill说明文档、数据库存储设计文档、部署调试文档、测试用例文档；
统一修正：旧逻辑描述、废弃功能、过时参数；和当前代码行为保持一致。

## 六、源码同步配套操作
1. 删除注释内所有版本号、过期临时调试说明；
2. 清理代码内遗留废弃注释、无效调试打印说明。

## 最终交付清单
1. 全部更新完成后的各级README、架构文档、Agent专项md、harness范式文档、memory.md备忘文档；
2. 完善后的简历项目详情md；
3. 《文档修改变更汇总清单》，记录每个md文件的主要改动点；
4. 注释优化完成的代码说明；

## 硬性约束重申
1. 全文所有文档、注释**不增加任何版本号信息**；
2. 所有改动仅修改文字文档与注释，不改动业务代码逻辑；
3. 文档结构具备良好检索性，支持后续定向修改代码时快速定位对应资料；
4. 禁止出现“通过Prompt优化模型”这类方案描述，统一强调所有问题采用底层代码架构修复。