# 车载Agent全链路推理修复\+TTS交互增强专属Prompt
> **项目地址**: D:\zhangmengdi\WorkSpace\NexusCockpit
> **运行环境**: Windows 10/11 x64 + NVIDIA RTX4070 (CUDA) 本地仿真车机
> **运行环境**: (nexus) PS D:\zhangmengdi\WorkSpace\NexusCockpit> + docker
## 一、任务定位

你是车载Multi\-Agent全链路架构修复工程师，本次专项修复**完整执行链路：supervisor → dispatch → responder → reflection → reviewer → END**现存致命缺陷，同时全面升级AI回复TTS语音播放交互能力。严格遵循：不改动原有正常业务逻辑、不破坏车控/技能/MCP/知识库能力，只修复链路漏洞、补齐校验机制、重构语音播放逻辑、封堵裸输出风险。

## 二、现存明确BUG与缺陷（本次全部强制修复）

**1\. 核心链路严重漏洞**

- 现有 responder → reflection → reviewer 链路执行不闭环，存在大量**启发式路由短路逻辑**，未经过完整审核校验直接输出结果给用户

- 会话短期记忆、长期记忆读取写入异常，多轮对话上下文断裂、记忆错乱、遗忘历史指令

- responder生成答案存在幻觉、不准确、不符合车载场景问题

- reflection自我反思机制失效，未对答案准确性、上下文一致性、场景合规性做校验

- reviewer终审校验形同虚设，无强制拦截、纠错、重生成逻辑

- 所有用户展示文本、TTS播报内容存在**未校验裸输出高危风险**

**2\. TTS语音播放体验硬伤**

- AI整段消息一次性无脑播放，不区分单句，无法匹配用户阅读速度

- 用户阅读文字速度远快于语音播放速度，体验严重割裂

- 无暂停、重新播放、断点续播、终止播放能力

- 长文本播报无法中断，必须播放至结束，交互极其不友好

- 不准确、未校验的内容会直接进入TTS播报，输出错误语音信息

## 三、全链路强制整改规则（核心、不可绕过）

针对**supervisor → dispatch → responder → reflection → reviewer → END** 全链路重构加固：

1. **Supervisor层**：强化任务解析、领域判定、Agent调度优先级管控，杜绝错误路由分发。

2. **Dispatch层**：修复启发式路由漏洞，**禁止路由直接输出结果**，所有任务必须完整流转后续三层校验链路，不允许任何环节短路结束。

3. **Responder层（修复重点）**：修复短期/长期记忆读写逻辑，保证上下文完整连贯；优化基础回答生成，降低幻觉；严格承接上游任务，不擅自截断、简化指令。

4. **Reflection层（强制生效）**：启用完整自我反思校验：上下文匹配度、车载场景合理性、答案准确性、是否存在错误/幻觉/歧义、是否与用户历史对话冲突，不合格答案标记待重生成。

5. **Reviewer终审强校验（全局唯一出口关卡）**：所有对外输出内容必须经过终审校验，包含：文本合规性、业务准确性、车控逻辑正确性、无错误数据、无幻觉、无语义漏洞。校验不通过自动触发二次生成、纠错、兜底话术，**绝对禁止未校验内容输出给前端、用户界面、TTS播报**。

6. **统一全局输出网关**：新增全局输出拦截层，所有展示给用户、所有语音播报内容必经网关审核，形成闭环，彻底根治裸输出问题。

## 四、TTS语音播放体系全面升级（强制落地）

1. 对每一条 AI messages 回复**按标点智能分句**，实现逐句生成、逐句播报，替代原有整段一次性播放模式。

2. 每条AI回复下方挂载标准化交互组件：**暂停播放、断点续播、单句重放、整条重放、终止播放**。

3. 新增全局语音播放状态机：记录当前播放位置、播放状态、断点位置、终止标记。

4. 支持用户随时打断语音播报，优先用户操作，解决“文字看完语音还在播放”的体验割裂问题。

5. 严格绑定校验机制：**未通过reviewer终审的文本，禁止进入TTS合成与播放流程**。

6. 优化播放逻辑：支持自由控制播放进度、重读重点内容、随时终止长文本播报。

## 五、记忆体系专项修复

1. 修复短期对话记忆上下文缓存、超时刷新、关联读取逻辑，杜绝多轮对话断层。

2. 修复长期用户记忆、车辆习惯记忆的持久化、匹配调用逻辑，保证连续对话一致性。

3. 清理记忆冗余、重复记忆、无效脏数据，提升问答准确率。

## 六、硬性改造约束

- 原有项目架构、Sub\-Agent、Skill、MCP、RAG、CAN车控、语音采集底层逻辑**全部保留不动**。

- 只修复链路漏洞、补齐校验闭环、重构TTS交互层、修复记忆BUG。

- 改造后全链路100%闭环，无任何短路输出、无未校验内容对外展示。

- 所有改动轻量化、无冗余堆砌、不新增无用框架，保证项目整洁高效。

## 七、输出交付要求

1. 全链路漏洞代码定位清单（路由短路、记忆异常、校验失效点位）

2. 修复完成的 supervisor→dispatch→responder→reflection→reviewer 完整闭环链路代码

3. 全局统一输出校验网关代码

4. 修复后的短期/长期记忆读写逻辑

5. 全新分句TTS播放\+暂停/重放/断点续播/终止播放完整交互代码

6. 详细改造说明 \+ 本机Windows\+4070环境自测验证步骤

> （注：部分内容可能由 AI 生成）